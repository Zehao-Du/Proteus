# 贡献指南 (Contributing Guide)

感谢您对 eBPF-TokenFlow 项目的关注！本文档将指导您如何提交 Pull Request (PR)。

## 📋 提交 PR 的步骤

### 1. Fork 仓库

首先，在 GitHub 上 Fork 本项目：
- 访问 https://github.com/Zehao-Du/eBPF-TokenFlow
- 点击右上角的 "Fork" 按钮

### 2. 克隆你的 Fork

```bash
git clone https://github.com/YOUR_USERNAME/eBPF-TokenFlow.git
cd eBPF-TokenFlow
```

### 3. 添加上游仓库（可选但推荐）

```bash
git remote add upstream https://github.com/Zehao-Du/eBPF-TokenFlow.git
```

这样可以方便地同步上游的更新：
```bash
git fetch upstream
git merge upstream/main
```

### 4. 创建功能分支

**重要**：永远不要直接在 `main` 分支上提交代码！

```bash
# 确保在 main 分支且是最新的
git checkout main
git pull upstream main

# 创建新分支（使用描述性的分支名）
git checkout -b feat/your-feature-name
# 或者
git checkout -b fix/your-bug-fix
# 或者
git checkout -b docs/update-readme
```

**分支命名规范**：
- `feat/` - 新功能
- `fix/` - Bug 修复
- `docs/` - 文档更新
- `refactor/` - 代码重构
- `test/` - 测试相关
- `chore/` - 构建/工具相关

### 5. 进行修改

进行你的代码修改，确保：
- ✅ 代码符合项目风格
- ✅ 添加必要的注释
- ✅ 更新相关文档（如 README.md）
- ✅ 测试你的修改

### 6. 提交更改

```bash
# 查看修改的文件
git status

# 添加修改的文件
git add <file1> <file2> ...

# 或者添加所有修改
git add .

# 提交（使用清晰的提交信息）
git commit -m "feat: 添加 ETPS 实验框架

- 创建 etps_experiment.py 用于性能对比
- 添加网络损伤模拟脚本
- 更新文档说明实验方法"
```

**提交信息规范**（遵循 Conventional Commits）：
- `feat:` - 新功能
- `fix:` - Bug 修复
- `docs:` - 文档更新
- `style:` - 代码格式（不影响功能）
- `refactor:` - 重构
- `test:` - 测试
- `chore:` - 构建/工具

### 7. 推送到你的 Fork

```bash
git push origin feat/your-feature-name
```

如果是第一次推送，使用：
```bash
git push -u origin feat/your-feature-name
```

### 8. 创建 Pull Request

1. 访问你的 Fork：https://github.com/YOUR_USERNAME/eBPF-TokenFlow
2. 你会看到 "Compare & pull request" 按钮，点击它
3. 填写 PR 描述：
   - **标题**：简洁描述你的改动
   - **描述**：详细说明：
     - 改动的目的和背景
     - 具体做了什么
     - 如何测试
     - 相关 Issue（如果有）

**PR 描述模板**：
```markdown
## 📝 改动说明

### 改动内容
- [ ] 功能 A
- [ ] 功能 B
- [ ] 文档更新

### 测试方法
1. 步骤 1
2. 步骤 2
3. 预期结果

### 相关 Issue
Closes #123

### 截图/演示
（如果有的话，添加截图或演示链接）
```

### 9. 等待审查

- 维护者会审查你的 PR
- 可能会要求修改或补充信息
- 根据反馈进行更新后，PR 会自动更新

## 🛠️ 使用自动化脚本

项目提供了一个便捷的 PR 提交脚本：

```bash
# 使用脚本提交 PR
bash submit_pr.sh
```

**注意**：使用前需要根据你的实际情况修改脚本中的：
- 分支名称
- 提交的文件列表
- 提交信息

## 📝 PR 检查清单

提交 PR 前，请确认：

- [ ] 代码可以正常运行
- [ ] 添加了必要的注释和文档
- [ ] 遵循了项目的代码风格
- [ ] 提交信息清晰明确
- [ ] 没有包含不必要的文件（如 `.pyc`, `__pycache__`）
- [ ] 更新了相关的 README 或文档
- [ ] 测试了你的修改

## 🚫 常见错误

### ❌ 在 main 分支直接提交
```bash
# 错误
git checkout main
git commit -m "..."

# 正确
git checkout -b feat/new-feature
git commit -m "..."
```

### ❌ 提交信息不清晰
```bash
# 错误
git commit -m "fix bug"

# 正确
git commit -m "fix: 修复 Hint Server 端口冲突问题"
```

### ❌ 包含不必要的文件
```bash
# 检查 .gitignore
# 确保不提交：
# - __pycache__/
# - *.pyc
# - .env
# - 日志文件
```

## 🔄 同步上游更新

如果你的 PR 需要等待一段时间，期间上游可能有新的提交：

```bash
# 1. 切换到你的功能分支
git checkout feat/your-feature-name

# 2. 获取上游更新
git fetch upstream

# 3. 合并上游的 main 分支
git merge upstream/main

# 4. 解决冲突（如果有）
# ... 编辑冲突文件 ...

# 5. 推送更新
git push origin feat/your-feature-name
```

## 📚 更多资源

- [GitHub Flow](https://guides.github.com/introduction/flow/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [如何写好 Git Commit Message](https://chris.beams.io/posts/git-commit/)

## ❓ 需要帮助？

如果遇到问题，可以：
- 在 GitHub Issues 中提问
- 查看项目的 README.md
- 联系项目维护者

---

再次感谢您的贡献！🎉

